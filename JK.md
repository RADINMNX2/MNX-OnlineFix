# راهنمای کامل MNX ONLINE FIX

## این برنامه چیست و چگونه کار می‌کند؟ (واقعیت ماجرا)

این برنامه یک ابزار پیشرفته برای **هدایت مجدد ترافیک شبکه بازی‌های کامپیوتری** است. ایده اصلی آن بر پایه یک تکنیک به نام **"DLL Proxying"** یا **"پراکسی کردن DLL"** بنا شده است.

### طرز کار در ۳ مرحله:

1.  **تزریق هوشمند (Smart Injection):**
    *   بسیاری از بازی‌هایی که از شبکه Steam استفاده می‌کنند، برای ارتباطات آنلاین خود به فایلی به نام `steam_api64.dll` وابسته‌اند.
    *   برنامه ما این فایل اصلی را در پوشه بازی پیدا می‌کند، نام آن را به `steam_api64_original.dll` تغییر می‌دهد (تا به عنوان پشتیبان باقی بماند) و سپس فایل `dll` سفارشی و هوشمند ما را با نام `steam_api64.dll` جایگزین آن می‌کند.

2.  **رهگیری و هدایت شبکه (Network Interception & Redirection):**
    *   حالا وقتی بازی اجرا می‌شود، به جای بارگذاری `dll` اصلی استیم، `dll` دستکاری شده ما را بارگذاری می‌کند.
    *   `dll` ما تمام توابع مهم شبکه (مثل `SendP2PPacket`) را در اختیار می‌گیرد. به جای اینکه بسته‌های اطلاعاتی بازی را به سرورهای استیم بفرستد، آن‌ها را از طریق یک سوکت UDP ساده به آدرس IP و پورتی که شما در رابط کاربری برنامه وارد کرده‌اید، ارسال می‌کند.
    *   این یعنی شما کنترل کامل ترافیک خروجی بازی را در دست دارید.

3.  **پل ارتباطی الکترون و C++ (Electron & C++ Bridge):**
    *   جادوی اصلی اینجاست. برنامه الکترون (رابط کاربری) از طریق یک **Node.js C++ Addon** با `dll` در حال اجرای ما ارتباط برقرار می‌کند.
    *   وقتی شما IP هدف را در برنامه وارد می‌کنید و دکمه "Connect" را می‌زنید، یک دستور از طریق الکترون به `addon` ما ارسال می‌شود. `addon` مستقیماً یک متغیر (`g_redirectIP`) را در حافظه `dll` که توسط بازی بارگذاری شده، تغییر می‌دهد. این کار باعث می‌شود که مقصد بسته‌های شبکه بازی **در لحظه** و بدون نیاز به ری‌استارت تغییر کند.

---

## راهنمای نصب و راه‌اندازی اولیه (فقط برای بار اول)

قبل از اجرای برنامه، باید بخش C++ آن را کامپایل کنید. این فایل `dll` هوشمند ما را می‌سازد.

1.  **نصب ابزارهای لازم:**
    *   ابتدا ابزارهای ساخت C++ برای ویندوز و Python را نصب کنید:
        ```bash
        npm install --global --production windows-build-tools
        ```
    *   سپس `node-gyp` و `node-addon-api` را نصب کنید:
        ```bash
        npm install -g node-gyp
        npm install node-addon-api
        ```

2.  **تغییر نام فایل Build:**
    *   فایل `binding.gyp.txt` را به `binding.gyp` تغییر نام دهید.

3.  **کامپایل پروژه:**
    *   در ترمینال، دستورات زیر را به ترتیب اجرا کنید:
        ```bash
        node-gyp configure
        node-gyp build
        ```
    *   اگر همه چیز موفقیت‌آمیز باشد، فایل نهایی در مسیر `build/Release/mnx_steam_proxy.node` ساخته می‌شود. **این فایل همان `dll` پراکسی شماست.**

---

## طرز صحیح استفاده از برنامه

1.  **اجرای برنامه:**
    *   با دستور `npm run dev` برنامه را در حالت توسعه اجرا کنید.
    *   برای ساخت فایل اجرایی (`.exe`)، از دستور `npm run dist` استفاده کنید.

2.  **افزودن بازی:**
    *   روی دکمه "Add Game" کلیک کرده و پوشه اصلی بازی مورد نظر خود را انتخاب کنید (پوشه‌ای که فایل `steam_api64.dll` در آن قرار دارد).

3.  **اتصال (Connect):**
    *   بازی مورد نظر را از لیست انتخاب کنید.
    *   آدرس IP سرور یا دوست خود را در فیلد IP وارد کنید.
    *   روی دکمه "Connect" کلیک کنید. در این لحظه:
        *   فایل `dll` اصلی بازی به `_original` تغییر نام پیدا می‌کند.
        *   فایل `mnx_steam_proxy.node` کامپایل شده شما به پوشه بازی کپی و به `steam_api64.dll` تغییر نام داده می‌شود.
        *   IP وارد شده به `dll` تزریق می‌شود.

4.  **بازیابی (Restore):**
    *   بعد از اتمام بازی، با کلیک بر روی "Disconnect" (که بعد از اتصال ظاهر می‌شود)، فایل `dll` اصلی بازی به حالت اولیه بازگردانده می‌شود.

---

## این برنامه جای کار بیشتری هم دارد (ایده برای آینده)

این پروژه یک فونداسیون بسیار قدرتمند است. در اینجا چند ایده برای توسعه آن وجود دارد:

*   **سیستم واقعی Room Discovery:** به جای لیست شبیه‌سازی شده، می‌توان یک پروتکل UDP ساده برای پیدا کردن خودکار بازیکنان دیگر در شبکه مجازی پیاده‌سازی کرد.
*   **حالت سرور میزبان (Host/Relay Server):** قابلیتی به برنامه اضافه شود که بتواند به عنوان یک سرور واسط (Relay) عمل کند تا دیگر نیازی به Port Forwarding نباشد.
*   **پشتیبانی از پروتکل‌های دیگر:** می‌توان با تحلیل پکت‌های بازی، پشتیبانی از پروتکل‌های پیچیده‌تر یا حتی TCP را اضافه کرد.
*   **رابط کاربری پیشرفته‌تر:** نمایش وضعیت دقیق اتصال، پینگ واقعی، لاگ‌های شبکه و... .
*   **پشتیبانی از DLLهای دیگر:** این ساختار را می‌توان برای پراکسی کردن `dll`های آنلاین دیگر (مانند `OnlineFix64.dll` و...) نیز گسترش داد.

موفق باشی! 🔥
**- RADINMNX**
